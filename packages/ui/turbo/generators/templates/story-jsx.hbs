import type { Meta, StoryObj } from '@storybook/react';

import {{pascelCase}} from "./{{pascelCase}}";

const meta: Meta<typeof {{pascelCase}}> = {
  title: "Components/{{pascelCase}}",
  component: {{pascelCase}},
};

export default meta;
type Story = StoryObj<typeof {{pascelCase}}>;

export const Primary: Story = {
  args: {
  },
};